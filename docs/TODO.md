- [x] Добавить опцию, чтобы был target на первое сообщение от пользователя. Пример: Я хочу пересылать каждое первое сообщение от каждого пользователя из папки "Coworkers". (реализовано как `once_per_chat` — первое совпадение на чат с дневным сбросом в `reset_hour`, PR #54)
- [x] Добавить настройку debounce_ms (по умолчанию 60000) для отправки сообщений. Когда сработал триггер, ждать debounce_ms после последнего отправленного в чат сообщения, затем переслать в target все сообщения, чьё время в пределах debounce_ms относительно сработавшего. Пример: 00:00:01 "Мне нужно создать аккаунт", 00:00:05 "Привет" (триггер по keywords), 00:00:55 "И почту" — в 00:01:55 уходят все 3 сообщения. (реализовано per-instance, по умолчанию 0/выкл, PR #54)
- [ ] Настраиваемое пересылаемое сообщение (configurable forwarded message) на уровне инстанса. Это сообщение уходит в агента (например Hermes) и должно содержать всю инфу об источнике. Сейчас формат фиксированный, например:
  ```
  word: привет

  Forwarded from: private @username, Name: User Name
  ```
  Нужно:
  - Возможность задавать собственный промпт/шаблон этого сообщения на уровне инстанса.
  - Флаги, чтобы включать/выключать любую часть сообщения: показывать "Forwarded from" (источник), показывать trigger (сработавшее слово/правило).
  - Переменные, чтобы добавить произвольный текст в начало и/или в конец сообщения.
